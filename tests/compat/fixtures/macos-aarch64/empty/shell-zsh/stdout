# meowctl shell integration for zsh
# Add this to your ~/.zshrc:
#   eval "$(meowctl shell zsh)"

meowctl_shell_init() {
    export MEOWCTL_SHELL=zsh
    [[ -n "$_MEOWCTL_SHELL_DONE" ]] && return
    export _MEOWCTL_SHELL_DONE=1
    eval "$(meowctl hook shell)"
}
meowctl_shell_init
